import React from 'react'
import NotebookIconButton from '../../common/icon_buttons/NotebookIconButton'

// The 'Move Note to...' menu.
function NoteHeaderMoveNotePanel () {
  return (
    <div>
      <NotebookIconButton />
      Notebook
    </div>
  )
}

export default NoteHeaderMoveNotePanel
